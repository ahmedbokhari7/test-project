{
  "channel":"WSClient",
  "clientId":"ERS",
  "clientRequestTimeout":"${clientRequestTimeout}",
  "id":"${id}",
  "prepareOnly":"${prepareOnly}",
  "type":"${type}",
  "userId":"${userId}",
  "password":"${password}",
  "userName" : "${userName}",
  "newPassword" : "${newPassword}"
}
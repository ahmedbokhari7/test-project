{
  "channel":"WSClient",
  "clientId":"ERS",
  "clientRequestTimeout":"${clientRequestTimeout}",
  "id":"${id}",
  "type":"${type}",
  "userId":"${userId}",
  "password":"${password}",
  "principalId":"${principalId}",
  "principalIdType":"${principalIdType}",
  "principalIdUserId":"${principalIdUserId}"
}
